{
  "name": "secretroom-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "check": "node --check index.js && node --check x-oauth.js && node --check x-oauth-start.js && node --check x-oauth-complete.js && node --check lib/config.js && node --check lib/http.js && node --check lib/member-auth.js && node --check lib/rate-limit.js && node --check lib/x-api.js",
    "serve": "npx firebase-tools@latest emulators:start --only functions",
    "deploy": "npx firebase-tools@latest deploy --only functions:xOAuthStart,functions:xOAuthComplete"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
