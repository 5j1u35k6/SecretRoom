name: Runtime cleanup reference

on:
  workflow_dispatch:

jobs:
  reference:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cleanup logic is executed by optimize-six-files.yml on the optimization branch."
