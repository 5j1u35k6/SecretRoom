name: Completed six-file consolidation

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The one-time consolidation has already completed on this branch."
